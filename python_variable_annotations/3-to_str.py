#!/usr/bin/env python3

"""Function takes a float n as argument and
returns the string representation of the float"""


def to_str(n: float) -> str:
    """Function takes a float n as argument and
    returns the string representation of the float"""
    return str(n)
